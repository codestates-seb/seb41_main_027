import styled from 'styled-components'

export const InfoLocationMap = styled.section`
  width: calc(100% - 4px);

  .map-static {
    height: 200px;
    background-color: #eee;
    border-radius: 12px;
    box-shadow: var(--box-shadow-list);
    overflow: hidden;
  }
`
